# ===========================================================================
# Coolify Environment Variables Template
# ===========================================================================
#
# Copy these to your Coolify Environment Variables section
# 
# REQUIRED: Generate SECRET_KEY_BASE with: openssl rand -hex 64
# REQUIRED: Set a strong POSTGRES_PASSWORD
#

# ===========================================================================
# REQUIRED VARIABLES
# ===========================================================================

# Rails secret key (REQUIRED - generate with: openssl rand -hex 64)
SECRET_KEY_BASE=

# Database configuration (REQUIRED)
POSTGRES_USER=sure_user
POSTGRES_PASSWORD=
POSTGRES_DB=sure_production

# ===========================================================================
# OPTIONAL VARIABLES
# ===========================================================================

# Application port (default: 3000, Coolify usually handles this)
PORT=3000

# Self-hosting mode (do not change unless you know what you're doing)
SELF_HOSTED=true

# Onboarding state: open (anyone can register), closed (no registration), invite_only
ONBOARDING_STATE=open

# ===========================================================================
# AI / CHAT FEATURES (OPTIONAL)
# ===========================================================================
# NOTE: OpenAI integration incurs costs. Set spend limits on your OpenAI account!

# OpenAI API key for AI features (chat, rules, insights)
OPENAI_ACCESS_TOKEN=

# Custom OpenAI-compatible endpoint (leave empty to use official OpenAI)
# Examples:
# - LM Studio: http://host.docker.internal:1234/v1
# - Ollama: http://host.docker.internal:11434/v1
OPENAI_URI_BASE=

# Model to use (leave empty for OpenAI default, or specify for custom endpoints)
# Examples: gpt-4, gpt-3.5-turbo, llama2, mistral
OPENAI_MODEL=

# Show detailed error messages in chat UI (useful for debugging)
AI_DEBUG_MODE=false

# Langfuse observability (optional)
LANGFUSE_PUBLIC_KEY=
LANGFUSE_SECRET_KEY=
LANGFUSE_HOST=https://cloud.langfuse.com

# ===========================================================================
# MARKET DATA (OPTIONAL)
# ===========================================================================

# Twelve Data API for investment/stock data
# Get your key at: https://twelvedata.com/
TWELVE_DATA_API_KEY=

# ===========================================================================
# BANK SYNC (OPTIONAL)
# ===========================================================================

# SimpleFIN configuration
SIMPLEFIN_DEBUG_RAW=false
SIMPLEFIN_INCLUDE_PENDING=false

# ===========================================================================
# EMAIL (OPTIONAL)
# ===========================================================================
# Configure SMTP for email notifications

SMTP_ADDRESS=
SMTP_PORT=587
SMTP_DOMAIN=
SMTP_USER_NAME=
SMTP_PASSWORD=
SMTP_AUTHENTICATION=plain
SMTP_ENABLE_STARTTLS_AUTO=true

# ===========================================================================
# OPENID CONNECT (OPTIONAL)
# ===========================================================================
# For SSO/OAuth login

OIDC_CLIENT_ID=
OIDC_CLIENT_SECRET=
OIDC_ISSUER=
OIDC_REDIRECT_URI=https://your-domain.com/auth/openid_connect/callback

# ===========================================================================
# ADVANCED CONFIGURATION (DO NOT CHANGE UNLESS NECESSARY)
# ===========================================================================

# Database connection
DB_HOST=db
DB_PORT=5432

# Redis connection
REDIS_URL=redis://redis:6379/1

# SSL configuration (Coolify handles SSL)
RAILS_FORCE_SSL=false
RAILS_ASSUME_SSL=false
